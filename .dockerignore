# 镜像构建时要排除的路径（让 `docker build` 的 context 小一些 + 防止泄漏 dev 数据）

# 依赖与构建产物（多阶段构建会重新装）
node_modules
.next
backend/dist
backend/*.tsbuildinfo
tsconfig.tsbuildinfo

# 本地 dev 数据
.dev-pg-data
*.bak-*

# 环境变量（绝不能进镜像）
.env
.env.local
.env.*.local

# 编辑器与 OS
.vscode
.idea
.DS_Store
Thumbs.db

# Git
.git
.gitignore
.github

# Docker 自身
Dockerfile.frontend
Dockerfile.backend
docker-compose*.yml
.dockerignore
Caddyfile

# 文档（占体积，运行时不需要）
*.md
deploy
scrapify.html
products_export.csv

# 测试 / 临时
coverage
*.log
